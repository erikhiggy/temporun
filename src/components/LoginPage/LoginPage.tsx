import React from 'react';
import { Button } from '@material-ui/core';

const LoginPage = () => (
  <div>
    <Button href="http://localhost:8888/login">Login through Spotify</Button>
  </div>
);

export default LoginPage;
